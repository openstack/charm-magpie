name: canary-layer
summary: Canary
maintainer: canary
description: |
        Canary layer - pings all peer units to validate network connectivity
tags: ["bigdata", "hadoop"]
peers:
  canary:
    interface: canary
